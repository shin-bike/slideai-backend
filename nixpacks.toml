[phases.setup]
nixPkgs = ["libreoffice", "poppler_utils", "python3", "python3Packages.pip"]

[phases.install]
cmds = ["pip3 install -r requirements.txt"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
