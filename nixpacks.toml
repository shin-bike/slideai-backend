[phases.setup]
nixPkgs = ["libreoffice", "poppler_utils", "python3"]

[phases.install]
cmds = ["pip3 install -r requirements.txt --break-system-packages"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
